name: live_translate_app
description: "BridgeCall - live video call translation with real-time subtitles."
publish_to: 'none'

version: 1.0.1+5
environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_webrtc: ^0.11.6
  web_socket_channel: ^3.0.1
  flutter_sound: ^9.2.13
  path_provider: ^2.1.4
  shared_preferences: ^2.2.3
  share_plus: ^10.0.2
  permission_handler: ^12.0.1
  flutter_tts: ^4.2.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"

flutter:
  uses-material-design: true
